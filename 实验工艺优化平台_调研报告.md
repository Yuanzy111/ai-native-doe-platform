# 实验 / 工艺优化平台调研报告

> 面向"用统计与优化方法减少昂贵实验、上层由大模型编排"的 AI-native 平台预研。
> 客观评估 BoFire、BayBE 两个开源框架能否作为基座,并与"仅借鉴""自研"路线对比。
> 报告中所有对框架能力的关键判断均已在本地源码 + 运行环境亲手核实(见附录 A/B)。

---

## 0. 一页速览(给决策者)

**问题本质**:这不是"训练一个大模型"的问题,而是**在实验成本极高、可做次数很少
(几十到几百次)的前提下,如何用最少实验找到最优配方/工艺参数**——统计学上叫
**实验设计(DoE)** 与 **贝叶斯优化(BO)**。核心算法是成熟的、有数学保证的,不是 LLM 能替代的。

**框架成熟度结论**:BoFire、BayBE 都是**工业级、活跃维护、Apache-2.0 可商用**的成熟框架,
底层同源(都建在 BoTorch/GPyTorch/PyTorch 上),各有侧重。二者的算法能力**足以覆盖**
配方优化、工艺参数寻优、实验设计这三类核心场景,自研这部分算法**性价比极低且风险高**。

**基座可行性结论**:两者都**可作为算法基座二次开发**,且扩展性比初判要好——
BoFire 有公开的 `register()` 插件式扩展接口、代理模型可持久化;BayBE 靠 cattrs 自动发现
子类,不 fork 也能加自定义组件。**但两者都只是"算法库",不含服务化、持久化、
多租户、权限、UI**——这些平台层能力需要你自建,这才是工作量真正所在。

**AI-native 结论**:可行,但 LLM 的正确位置是**编排层与人机接口**(把自然语言意图翻译成
严谨的问题定义、选策略、解读 SHAP/收敛结果、生成报告),**不应让 LLM 直接产出实验推荐**
——推荐必须来自 BO/DoE 算法,否则丧失可信性、可复现性与数学保证,而这恰恰是"减少昂贵
实验"的价值根基。

**产品形态结论**:**混合形态(GUI 为主 + 对话副驾)** 优于纯对话。领域专家(化学/工艺
工程师)按表格、参数区间、约束、响应曲面思考;纯对话难以严谨录入优化问题、也难以呈现
不确定度与 Pareto 权衡。对话的价值在降低上手门槛与解读结果,而非取代结构化界面。

**一句话建议**:**以 BayBE 或 BoFire 为算法基座(倾向双后端抽象,先落 BayBE),自研平台层
与 AI 编排层,产品形态走"GUI + 对话副驾"**。不要自研核心 BO/DoE 算法,也不要让 LLM 代替算法做推荐。

---

## 1. 需求解构:这到底是个什么问题

### 1.1 问题的数学本质
公司的诉求——"有限实验数据下,优化实验规划或工艺配方,尽量少做实验找到更优方案"——
在统计/运筹领域是一个定义清晰的问题族:

- **实验设计(Design of Experiments, DoE)**:在**还没有任何数据**时,如何布置最少的实验点
  以最大化信息量(D/A/I/G-最优设计、因子设计、拉丁超立方等)。用于冷启动。
- **贝叶斯优化(Bayesian Optimization, BO)**:在**已有少量数据**时,用代理模型(通常高斯过程 GP)
  拟合"参数→响应"的关系并给出不确定度,再用采集函数(EI/UCB/qNEHVI 等)权衡"探索 vs 利用",
  推荐下一批最值得做的实验。这是"少实验找最优"的核心引擎。
- **多目标优化**:同时优化多个目标(如收率↑、成本↓、杂质↓),求 Pareto 前沿。
- **带约束优化**:配方类问题几乎必然带约束(如各组分配比之和=1 的混合约束、
  某些组分最多选 k 种的 N-choose-K 约束、线性/非线性工艺约束)。

### 1.2 关键含义
1. **核心价值在算法,不在大模型**。"减少实验次数"的数学保证来自 BO/DoE 的采集函数与代理模型,
   这是几十年的成熟理论。LLM 无法提供这种"下一个实验做哪里"的严谨推荐。
2. **成本不对称是设计前提**。一次真实实验(合成/中试/表征)可能耗时数天、花费上万,而算法
   计算是秒级。所以平台应"重算法决策、轻计算成本",宁可多花算力选点,也要少做实验。
3. **人始终在环上(human-in-the-loop)**。实验由人做、结果由人测,平台是"推荐→人执行→回填→再推荐"
   的闭环助手,不是全自动系统。这决定了产品必须有让人高效录入/审阅的界面。
4. **领域知识注入价值大**。工程师往往有先验(某区间更可能好、某组合有风险)。如何让专家把
   知识喂给算法(初始点、约束、迁移学习历史项目),是平台差异化的关键——这里 LLM 有用武之地。

---

## 2. 两个候选框架的客观评估

两者均 **Apache-2.0(可商用)**、要求 **Python ≥3.10**、底层同为 **BoTorch/GPyTorch/PyTorch**,
社区活跃(均有企业背书:BoFire 出自 BASF,BayBE 出自 Merck/默克)。规模相近(各约 450 个
Python 文件、7 万余行)。下面聚焦**架构差异**与**作为平台基座的适配度**。

### 2.1 BoFire(BASF)—— "可序列化的问题规格 + 求解器工厂"

**设计范式**:严格的**双层分离**。
- `bofire.data_models.*`:全部是 **Pydantic** 声明式数据模型(`Domain = Inputs + Outputs +
  Constraints`、各类 `Strategy` spec),**可完整序列化成 JSON**,用 `type` 字段做多态判别。
- `bofire.strategies/surrogates/kernels/*`:功能实现,通过 `mapper.map(data_model)` 把 spec
  落地成可执行对象。

**运行流程**:`domain → strategy spec → map() → strategy.tell(实验DataFrame) → strategy.ask(n)`。
`tell` 全量重拟合;`ask` 返回带**预测均值/标准差/期望度**(`_pred/_sd/_des`)的候选。

**核心优势**:
- **约束与实验设计(DoE)最强**:内置 D/A/E/G/I/K-最优设计(`DoEStrategy`,用 cvxpy/IPOPT),
  支持混合约束、N-choose-K、非线性约束、Interpoint 约束——**配方类问题的刚需**,BayBE 无对等的
  最优设计模块。
- **问题定义纯 spec 化 → REST 友好**:整个优化问题可表示成 JSON,天然适配"前端表单→后端执行"
  的平台形态(这正是其架构初衷)。
- **可扩展性好(已核实)**:提供公开 `register()` / `register_strategy()` + `tagged_union` 机制,
  **不 fork 即可注册自定义策略/代理/核**(附录 A)。
- **模型可持久化(已核实)**:代理模型有 `dumps()/loads()`(torch.save+cloudpickle),
  可缓存已拟合模型,避免每轮重训(附录 A)。
- **可解释性**:`surrogates/feature_importance.py` 提供 SHAP、lengthscale ARD、置换重要性。
- 还内置了实验性的 `LLMStrategy`(用 LLM 直接提议候选,依赖 pydantic-ai)——**本平台不建议
  用它做推荐器**(见 §4),但其 provider 抽象层可复用。

**风险 / 短板**:
- **导入重且首次慢**:`strategies.api` 会拉起 shap→numba,首次导入触发 JIT 编译,数十秒;
  多进程并发导入还会争用 numba 缓存锁(本次调研踩过坑)。平台需常驻进程 + 预热缓存规避。
- **无服务层**:纯算法库,不含 HTTP/持久化/多租户,全需自建。
- **状态在调用方**:strategy 每轮需 `tell` 全量历史,平台要自己存实验数据。

### 2.2 BayBE(Merck)—— "自包含、可存档的实验活动对象"

**设计范式**:**attrs 不可变值对象 + 单一有状态 `Campaign`**。
- 领域对象(参数/目标/约束/推荐器)都是 `@define(frozen=True)` 不可变值对象。
- **`Campaign = SearchSpace + Objective + Recommender`** 是唯一的有状态类,持有全部测量历史。
- 序列化用 **cattrs + SerialMixin**,多态靠抽象基类的 `type` 字段自动发现子类。

**运行流程**:`SearchSpace.from_product([...]) + Objective → Campaign → campaign.recommend(n)
→ 回填目标列 → campaign.add_measurements(df)`。`add_measurements` **增量累积**,无需每轮重传历史。

**核心优势**:
- **状态自包含 → 平台化成本极低**:一个 `Campaign.to_json()` 就装下搜索空间、目标、推荐器配置
  **和全部测量历史**(单个 JSON blob),`from_json()` 原样恢复。天然适合"实验活动=一条可存档
  记录"的服务化模型:存库→加载→recommend→再存库,服务可无状态(附录 B)。
- **回测/仿真模块(独有)**:`simulation.simulate_experiment / simulate_scenarios` 可在已知函数
  或查找表上做闭环回测与蒙特卡洛,**"先验证策略能省多少实验、再投真实验"**——这是量化平台
  价值、说服业务方的利器,BoFire 无对等模块。
- **迁移学习一等公民**:`TaskParameter` + 多任务 GP,复用历史项目/相近产品的数据。配方场景常见
  "换个基材重做一遍",迁移学习能进一步省实验。
- **化学/物质编码**:`SubstanceParameter`(需 rdkit,MORDRED/指纹编码),药物/催化剂/配方筛选开箱即用。
- **可扩展性好(已核实)**:cattrs 结构化钩子自动发现子类,自定义代理/推荐器**不 fork 即可
  被序列化识别**;自定义代理还支持 ONNX(附录 B)。
- **可解释性**:`insights/shap.py`(SHAP/LIME/MAPLE + 多种图)。
- **无遥测(已核实纠正)**:当前版本源码中**没有任何遥测上报代码**;唯一的外网调用是可选的
  化学名→SMILES 查询(仅在用户主动调用该 helper 时触发)。早期版本曾有遥测,现已移除。

**风险 / 短板**:
- **离散空间会物化全笛卡尔积**:`SearchSpace.from_product` 把离散子空间展开成 DataFrame
  (`exp_rep`),高基数离散/类别组合可能**内存爆炸**。BayBE 自带 `estimate_product_space_size`
  内存预估作护栏,但平台设计需对高维离散场景做限流/改用连续参数(附录 B)。
- **无最优设计(DoE)模块**:冷启动靠初始随机/推荐器探索,缺少 BoFire 的 D-最优等严谨设计。
- **无服务层**:同 BoFire,平台层需自建。
- **序列化无显式版本号**:跨 BayBE 版本长期存档的 Campaign 有兼容性风险,平台需自加 schema 版本管理。

### 2.3 关键维度对照

| 维度 | BoFire | BayBE |
|---|---|---|
| 设计范式 | Pydantic 双层分离(spec ↔ 实现) | attrs 值对象 + 单一有状态 Campaign |
| 核心接口 | `tell(全量)/ask()` | `recommend()/add_measurements(增量)` |
| 状态管理 | 调用方持有实验数据 | Campaign 自包含全状态 |
| 序列化 | Domain/Strategy spec 可 JSON;模型另有 dumps/loads | 整个 Campaign 一个 JSON(含历史) |
| 实验设计 DoE | **强**(D/A/G/I/K-最优等) | 无专门模块 |
| 约束能力 | **强**(混合/N-choose-K/非线性/Interpoint) | 线性(连续/离散)为主 |
| 多目标 | qNEHVI/qParEGO | Desirability / Pareto |
| 回测仿真 | 无 | **强**(simulate_experiment/scenarios) |
| 迁移学习 | 多任务 GP | **一等公民**(TaskParameter) |
| 化学编码 | MolecularInput | SubstanceParameter(rdkit) |
| 可解释性 | SHAP/ARD/置换 | SHAP/LIME/MAPLE |
| 扩展性 | `register()` 插件式(不 fork) | cattrs 自动发现(不 fork) |
| REST 友好度 | **高**(纯 spec) | 中(靠 Campaign JSON) |
| 平台化就绪度 | 中(状态外置) | **高**(状态自包含) |
| 主要风险 | 导入慢、无服务层 | 离散空间内存、无 DoE、无版本号 |

**互补性**:BoFire 强在**约束/DoE/REST 化问题定义**,BayBE 强在**自包含状态/回测/迁移学习**。
二者不是替代关系。若只选一个:**平台化优先选 BayBE(状态模型最省事),约束/DoE 优先选 BoFire。**

---

## 3. 路线对比:采用基座 vs 仅借鉴 vs 自研

| 路线 | 做法 | 优势 | 劣势 / 风险 | 适用判断 |
|---|---|---|---|---|
| **A. 直接采用基座** | 以 BayBE/BoFire 为算法引擎,自研平台层+AI层 | 上手最快(核心算法零开发)、跟随上游更新、社区验证 | 受上游 API 演进牵制、需吃透其模型、部分能力要绕开短板 | **推荐**。算法非公司核心壁垒时 |
| **B. 抽象双后端** | 自研一层框架无关接口,BayBE/BoFire 作可插拔后端 | 各取所长(BoFire 约束/DoE + BayBE 回测/状态)、不被单一上游绑定、未来可换引擎 | 抽象层有工程成本、需处理两后端能力不对称 | **推荐(进阶)**。中长期平台化 |
| **C. 仅借鉴 + 自研核心** | 读其思路,自己实现 BO/DoE | 完全可控、无外部依赖 | **重复造轮子、成本极高、易出数学错误**、追平 BoTorch 生态需数人年 | 不推荐,除非算法是核心壁垒且有专家团队 |
| **D. 纯自研(含底层 GP)** | 从 GP/采集函数写起 | 理论上最灵活 | 成本与风险最高,GPyTorch/BoTorch 已是事实标准 | **强烈不推荐** |

**结论**:算法层用 A 或 B(不自研),把自研精力投在**平台层(服务化/持久化/多租户/权限/审计)
+ AI 编排层 + 领域适配(公司真实配方/工艺场景的参数/约束模板、数据对接)**——这些才是
公司能建立差异化壁垒、且开源框架不提供的部分。

**A 还是 B?** 建议**先 A 后 B**:MVP 阶段直接用单一后端(BayBE,状态自包含最省事)跑通闭环,
验证价值;确认要长期投入后,再抽出框架无关接口演进为 B(把 BoFire 的 DoE/强约束补进来)。
一开始就做 B 会拖慢价值验证。

---

## 4. AI-native:大模型应该放在哪一层

你的原始想法——"底层各类统计/优化算法,上层由大模型组织、协调计算与预测"——方向正确,
但需要**精确划定 LLM 的职责边界**,否则会砸掉整个方案的可信度。

### 4.1 LLM 该做的(编排层 + 人机接口)
1. **意图形式化**:把工程师的自然语言("我想在这三个组分里找收率最高的配方,组分和为 1,
   成本别超预算")翻译成**严谨的问题定义**(参数/边界/约束/目标),供人确认后交给算法。
2. **策略/配置推荐**:根据问题特征(几个参数、连续/离散、单/多目标、有无约束)建议用哪个
   后端、哪种策略、批大小,并**解释理由**。
3. **结果解读**:把算法输出的候选点、预测不确定度、SHAP 特征重要性、收敛曲线、Pareto 前沿
   翻译成**人话**("模型认为温度是最关键因素;推荐的第 2 个点风险高但潜在收益大")。
4. **报告与知识沉淀**:自动生成实验轮次报告;把历史项目组织成可检索知识(RAG),供迁移学习
   或新项目冷启动参考。
5. **流程编排(Agent)**:以工具调用(function-calling / MCP)驱动"定义→推荐→回填→分析"闭环,
   在合适节点提示人介入。

### 4.2 LLM 不该做的(算法决策)
- **绝不让 LLM 直接产出"下一个实验做哪里"的推荐**。推荐必须来自 BO/DoE 算法。理由:
  - LLM 的输出不可复现、无不确定度量化、无收敛保证——而"少做实验"的价值恰恰依赖这些数学性质;
  - 一旦让 LLM 拍脑袋选点,平台就退化成"高级聊天机器人",丧失相对 Excel/人工试错的核心优势;
  - 出了坏实验结果无法归因(是算法问题还是模型幻觉?)。
- BoFire 虽内置 `LLMStrategy`(LLM 直接提议候选),**本平台不采用它做主推荐器**;至多在
  "完全冷启动 + 强领域先验"的探索场景做补充,且需明确标注"非算法推荐"。

### 4.3 一个健康的分层
```
   自然语言意图 / 结果解读 / 报告        ← LLM 编排层(可信边界:只翻译与解释,不做数值决策)
        ↕ 结构化调用(工具/MCP)
   问题定义 · 推荐 · 回填 · 分析 服务     ← 平台服务层(自研:状态、持久化、权限、审计)
        ↕ 框架无关接口
   BO / DoE / GP / 采集函数 / SHAP        ← 算法引擎层(BayBE / BoFire,不自研)
```
**关键原则**:数值从算法层流出后,LLM 只做"透传 + 解释",绝不改写候选点的数值。所有会改变
优化状态的写操作(定义问题、录入实验结果)由人确认。这样既 AI-native、又守住可信底线。

---

## 5. 产品形态:纯对话 vs 图形 UI

你纠结的"纯 NX 对话式 vs 配图形 UI"——**建议混合:GUI 为主承载结构化操作,对话作副驾**。

### 5.1 为什么纯对话不够
- **优化问题需要严谨录入**:十几个参数的边界、单位、多条约束、多个目标权重——用自然语言
  一句句说极易出错、难核对。表格/表单是工程师熟悉且不易错的载体。
- **结果本质是可视的**:响应曲面、收敛曲线、Pareto 前沿、不确定度带、SHAP 图——这些是二维/多维
  的视觉信息,塞进对话气泡里表达力大打折扣。
- **领域专家的心智模型是表格化的**:化学/工艺工程师习惯 DOE 表、参数矩阵,而非聊天流。

### 5.2 为什么纯 GUI 也不够
- 上手门槛高:不懂 BO/DoE 的工程师面对一堆策略/采集函数参数会懵。
- 结果需要解读:一张 SHAP 图,新手看不懂"这说明什么、下一步该怎么办"。

### 5.3 推荐形态:混合 co-pilot
- **GUI 主界面**承载:(a) 问题定义表单;(b) 实验数据表(回填结果);(c) 推荐候选视图
  (带预测/不确定度);(d) 可视化(收敛/响应面/Pareto/SHAP)。
- **对话副驾**贴在侧边,能:把自然语言变成表单草稿、解释任一图表、建议下一步、生成报告。
- **二者共享同一优化项目状态**(对话里产生的候选立刻出现在数据表)——这点 BayBE 的
  自包含 Campaign 状态模型天然支持。

### 5.4 技术选型建议
- **MVP 阶段用 Streamlit**:交付最快,BayBE 自己就带 Streamlit 参考 app 可借鉴;业务逻辑全在
  服务层,UI 仅展示。**不建议 MVP 直接上 React+FastAPI**(拖慢价值验证)。
- **产品化阶段迁 React + FastAPI**:届时算法/服务层零改动,只换前端与 API 网关。
- 若最终要"解包成公司已有 active 平台的底层能力":把**算法引擎层 + 服务层**做成框架无关、
  可作为工具/微服务被调用的形态,UI 由公司平台统一承载即可。

---

## 6. 主要风险与规避

| 风险 | 说明 | 规避 |
|---|---|---|
| 首次导入慢/numba 锁争用 | BoFire 拉 shap→numba,冷启动数十秒,多进程抢缓存锁 | 服务常驻进程、预热并固定 `NUMBA_CACHE_DIR`、避免并发首次导入 |
| 离散空间内存爆炸 | BayBE 物化全笛卡尔积 | 用其内存预估护栏;高维离散改连续参数或分层建模 |
| 上游 API 演进 | 跟随开源版本可能破坏兼容 | 锁定版本 + 抽象层隔离 + 回归测试 |
| 序列化无版本号(BayBE) | 长期存档跨版本兼容风险 | 平台自加 schema 版本 + 迁移脚本 |
| LLM 越权做数值决策 | 丧失可信/可复现 | 架构上禁止(§4.2),数值透传、写操作人工确认 |
| 冷启动 BoFire 抛错 | 实验不足时 `ask()` 抛 ValueError(已核实 `strategy.py:156`) | 冷启动回退 RandomStrategy/DoE,适配层兜底 |
| 数据/配方涉密 | 工业配方是核心资产 | 本地/私有化部署;LLM 用私有模型或做数据脱敏 |

---

## 7. 建议的落地路径(增量、可独立验证)

1. **MVP(验证价值)**:单后端(BayBE)+ Streamlit,跑通"定义→推荐→回填→再推荐→解读"闭环;
   用 BayBE 回测模块量化"BO 比随机/人工试错省多少实验"作为价值证据。
2. **AI 编排接入**:加对话副驾(意图→问题定义、结果解读、报告生成),LLM 只编排不决策。
3. **平台化**:自研服务层(项目持久化 SQLite→Postgres、多用户、权限、审计),抽出框架无关接口。
4. **双后端 + 强约束/DoE**:接入 BoFire 补齐最优设计与复杂约束,按问题特征自动选后端。
5. **领域深化**:沉淀公司真实场景的参数/约束模板、历史项目库(迁移学习 + RAG)。

---

## 附录 A:BoFire 关键能力的亲手核实
- **可插件式扩展(不 fork)**:`bofire/strategies/mapper.py` 提供公开 `register()`(装饰器/函数两用);
  `bofire/data_models/strategies/_register.py::register_strategy` + `data_models/unions.py::tagged_union`
  支持运行时把自定义策略注册进多态联合。运行验证:`register`、`register_strategy`、`tagged_union` 均可调用。
- **模型可持久化**:`bofire/surrogates/surrogate.py:106` 有 `dumps()/loads()`;具体实现
  (`botorch.py`、`random_forest.py`、`empirical.py`)用 `torch.save`(+cloudpickle)。运行验证:
  `Surrogate` 具备 `dumps/loads`。→ **纠正了"每轮必须重训、无法持久化"的初判**。
- **冷启动抛错**:`bofire/strategies/strategy.py:156-159`,实验不足时 `ask()` 抛 ValueError(需适配层兜底)。

## 附录 B:BayBE 关键能力的亲手核实
- **无遥测**:全包 grep 无任何 telemetry/opentelemetry 代码;唯一外网调用是 `utils/chemistry.py`
  可选的化学名→SMILES 查询(仅用户主动调用触发)。→ **纠正了"默认遥测上报"的说法**(早期版本有,现已移除)。
- **可扩展(不 fork)**:`baybe/serialization/core.py::make_base_structure_hook` + `get_subclasses`
  自动发现子类,自定义组件导入后即可被序列化识别。
- **离散空间物化**:`baybe/searchspace/discrete.py` 的 `exp_rep` 是展开后的 DataFrame;
  `searchspace/core.py:455` 提供 `estimate_product_space_size` 内存预估护栏。
- **状态自包含**:`Campaign.to_json/from_json` 含全部测量历史(base64 pickle),单 JSON 可存档恢复
  (本次实测往返一致)。

## 附录 C:调研过程说明
- 环境:micromamba 环境 `bo_examples`(Python 3.11 + CPU 版 torch 2.13 + botorch 0.18.1),
  两框架**源码零改动**,经 `PYTHONPATH` 指向本地源码运行。
- 另见 `comparison_report.md`(技术对比细节)及两个可运行最小示例
  `bofire/examples_minimal/`、`baybe/examples_minimal/`。
- **重要提示**:本报告纠正了初步调研中三处与事实不符的结论(BoFire 扩展性、BoFire 模型持久化、
  BayBE 遥测),均已在本地源码/运行环境亲手复核。若后续上游版本变化,相关结论需重新验证。
